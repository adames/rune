[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "chord-cheatsheet"
version = "0.1.0"
description = "A unified keybinding cheatsheet generated from your configs — which-key for your whole machine"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "adames" }]
keywords = ["cheatsheet", "keybindings", "dotfiles", "tmux", "neovim", "which-key", "hud"]

[project.scripts]
chord = "chord.cli:main"

[project.urls]
Homepage = "https://github.com/adames/chord"

[tool.setuptools]
packages = ["chord", "chord.extractors", "chord.render"]
